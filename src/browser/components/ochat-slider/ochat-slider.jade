.wrapper
    ol(style="width: {{images.length * 100}}%")
        li("*ngFor"="#img of images").image
            img(src = "{{img.src}}", alt = "{{img.title}}", style="max-width: 100%; max-height: 100%")
    menu
        button("(click)" = "prev()").prev
        button("(click)" = "next()").next
