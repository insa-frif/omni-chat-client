div.slider
	h1 {{title}}
	div("*ngIf"="pictures && currentPic")
		div("*ngFor"="#pic of pictures ; #i=index", "[class.current]"="i === currentPic", "[class.hidden]"="i !== currentPic")
			img.pic(src="{{pic}}", "(click)"="onClick()")
