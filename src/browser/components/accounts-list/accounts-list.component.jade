md-card
  .user-details("*ngIf"="user !== null")
    h2 {{user.name | async}}
    //ul
    //  li("*ngFor"="let acc of user.accounts | async")
    //    h2
    //      span {{acc.name}}
    //      span {{acc.driver}}

  .top-buttons
    button(md-icon-button="", title="Add account", "(click)"="addAccount()")
      md-icon.md-24 add
    = " "
    button(md-icon-button="", title="New MetaDiscussion")
      md-icon.md-24 forum
    = " "
    a(md-icon-button="", "[routerLink]"="['/chat']", title="Home")
      md-icon.md-24 home

  // h2 Meta
    ul
      li(ngFor...)
  // ul
    li("*ngFor"="let acc of accounts | async")
      h2
        span {{acc.name}}
        span {{acc.life}}
  // ul
    li("*ngFor"="let acc of contacts")
      h2
        span {{acc.username}}
        // TODO: add an icon here to show the driverName
      .contacts
        md-nav-list
          // todo: add parameters to routerLink (['/chat/discussion', discussionId])
          a(md-list-item="", "[routerLink]"="['/chat/discussion']")
            .avatar
            .name Contact1
          a(md-list-item="", "[routerLink]"="['/chat/discussion']")
            .avatar
            .name Contact2

// md-sidenav-layout(layout="row")
  md-sidenav("[mode]"="'side'", opened="true")
//md-toolbar
//  h1 Your contacts
//// md-input(type="text", "[(ngModel)]"="searchQuery", placeholder="Search...")
//h2.md-no-sticky Recent...
//md-list
//  md-list-item("*ngFor"="let c of contacts", "[class.selected]"="c === selectedContact",
//    "(click)"="selectContact(c)")
//    h3.name {{c.username}}
//    button("*ngIf"="c === selectedContact", "md-raised-button"="", "(click)"="sendEvent()" ) Discuss
